# ไฟล์อธิบายการเปิดข้อมูลต่างๆ

วิธีเปิดระบบ และวิธีเข้าไปดูข้อมูลดิบทุกจุด: ฐานข้อมูล, API, log ของ container, ไฟล์ JSON ตัวอย่าง, ใบรับรอง, ไฟล์ตั้งค่า

ทุกคำสั่งรันใน **Git Bash** ที่โฟลเดอร์ `E:\Demo_test` (`cd /e/Demo_test`) เว้นแต่จะบอกว่ารันบน VM

---

## สารบัญ

1. [เปิด / ปิดระบบ](#1-เปิด--ปิดระบบ)
2. [ฐานข้อมูล](#2-ฐานข้อมูล)
3. [API](#3-api)
4. [ส่ง log เข้าระบบ](#4-ส่ง-log-เข้าระบบ)
5. [ไฟล์ JSON และ log ตัวอย่าง](#5-ไฟล์-json-และ-log-ตัวอย่าง)
6. [log ของ container](#6-log-ของ-container)
7. [ไฟล์ตั้งค่า, ใบรับรอง, GeoIP, volume](#7-ไฟล์ตั้งค่า-ใบรับรอง-geoip-volume)
8. [สำรองและส่งออกข้อมูล](#8-สำรองและส่งออกข้อมูล)
9. [เทสต์](#9-เทสต์)
10. [สิ่งที่ห้ามทำ](#10-สิ่งที่ห้ามทำ)

---

## 1. เปิด / ปิดระบบ

### 1.1 ในเครื่อง (Docker)

ต้องเปิด Docker Desktop ก่อน (ถ้าค้าง ให้รีสตาร์ท Windows)

| คำสั่ง | ผล |
|---|---|
| `./run.sh` | build + เปิดโหมด SaaS + ใส่ข้อมูลเดโม |
| `./run.sh appliance` | เปิดโหมด Appliance (เข้าได้เฉพาะเครื่องนี้) |
| `./run.sh down` | ปิด เก็บข้อมูลไว้ |
| `./run.sh clean` | ปิดและ **ลบข้อมูลทั้งหมด** |
| `docker compose ps` | ดูสถานะ container |
| `docker compose restart backend` | รีสตาร์ทเฉพาะ backend |

| URL | หมายเหตุ |
|---|---|
| <http://localhost:8081> | หน้าเว็บ |
| <https://localhost:8443> | หน้าเว็บ TLS self-signed (เบราว์เซอร์เตือนครั้งแรก) |
| <http://localhost:8080/api/health> | backend ตรง ไม่ผ่าน nginx |
| `localhost:5433` | PostgreSQL |
| `localhost:514` UDP/TCP | syslog |

### 1.2 โหมดพัฒนา (แก้โค้ดแล้วเห็นผลทันที)

```bash
docker compose up -d db
```

```bash
cd backend && npm install && npm run dev
```

เปิดอีกหน้าต่าง:

```bash
cd frontend && npm install && npm run dev
```

หน้าเว็บอยู่ที่ <http://localhost:5173> (ส่ง `/api` ต่อไปที่ `:8080` ให้เอง)

### 1.3 บน Azure

| ขั้น | วิธี |
|---|---|
| เปิด | <https://portal.azure.com> → **VMdemo** → **Start** รอ 2–3 นาที |
| เข้าเว็บ | <https://siem-demo.eastasia.cloudapp.azure.com> |
| SSH | `ssh -i <ไฟล์-ssh-key> azureuser@<IP-ของ-VM>` |
| สถานะบน VM | `cd ~/mini-siem && sudo docker compose -f docker-compose.yml -f docker-compose.caddy.yml ps` |
| อัปเดตโค้ด | `git pull && sudo docker compose -f docker-compose.yml -f docker-compose.caddy.yml up -d --build` |
| ปิด | Portal → VMdemo → **Stop** |

### 1.4 บัญชี

รหัสผ่าน: Admin `admin123` · Viewer `viewer123`

ฐานข้อมูลที่ seed ไว้ก่อนหน้ายังใช้รหัสเดิมจนกว่าจะรัน `docker compose exec -T backend npm run seed:prod -- --accounts-only` (เปลี่ยนเฉพาะรหัสผ่าน ไม่เพิ่ม log)

การล็อกอินหน้าเว็บทุกครั้ง (สำเร็จ, รหัสผิด, อีเมลที่ไม่มี) ถูกบันทึกเป็น log ใน tenant **Mini SIEM** — Admin เลือก tenant นี้ที่ Dashboard / Search เพื่อดู · พิมพ์รหัสผิด 5 ครั้งใน 5 นาทีจะเกิด alert และบัญชีนั้นถูกล็อก 15 นาที (ตอบ 429)

| อีเมล | บทบาท |
|---|---|
| `admin@siem.local` | Admin — ทุก tenant |
| `viewer@northwind.local` | Viewer — Northwind Traders |
| `viewer@contoso.local` | Viewer — Contoso Ltd |

---

## 2. ฐานข้อมูล

### 2.1 เข้าผ่านคำสั่ง

ในเครื่อง:

```bash
docker compose exec db psql -U postgres -d siem
```

บน VM (ฐานข้อมูลไม่ได้เปิดพอร์ตออกนอก ต้อง SSH เข้าไปก่อน):

```bash
sudo docker exec -it mini-siem-db-1 psql -U postgres -d siem
```

### 2.2 เข้าผ่านโปรแกรม GUI (DBeaver / pgAdmin / TablePlus) — เฉพาะในเครื่อง

| ช่อง | ค่า |
|---|---|
| Host | `localhost` |
| Port | `5433` |
| Database | `siem` |
| User | `postgres` |
| Password | บรรทัด `POSTGRES_PASSWORD=` ในไฟล์ `.env` |

### 2.3 ตาราง

| ตาราง | เก็บอะไร |
|---|---|
| `events` | log ทุกบรรทัดหลังแปลงแล้ว (แบ่ง partition ตามวันและ tenant) |
| `tenants` | ลูกค้า |
| `users` | ผู้ใช้ (รหัสผ่านเก็บเป็น hash) |
| `sessions` | session ที่ล็อกอินอยู่ (เก็บ hash ของ token) |
| `collectors` | ช่องทางรับ log ของแต่ละ tenant (token เก็บเป็น hash, CIDR ของ syslog) |
| `alert_rules` | กฎแจ้งเตือน |
| `alerts` | alert ที่เกิดขึ้น |
| `audit_log` | การกระทำของผู้ดูแล |
| `ingest_drops` | ข้อมูลที่ถูกปฏิเสธ (ผู้ส่งไม่รู้จัก, batch ใหญ่เกิน) |
| `schema_migrations` | migration ที่รันแล้ว และ checksum |

### 2.4 คำสั่งดูข้อมูลดิบ

```sql
\dt                                   -- รายชื่อตาราง
\d events                             -- โครงสร้างตาราง events
```

```sql
-- log ล่าสุดพร้อมข้อมูลดิบที่รับเข้ามา
SELECT ts, source, event_type, event_outcome, user_name, src_ip, raw
FROM events ORDER BY ts DESC LIMIT 10;
```

```sql
-- ข้อมูลเสริมที่ parser แยกไว้ (jsonb)
SELECT event_type, jsonb_pretty(attrs) FROM events ORDER BY ts DESC LIMIT 3;
```

```sql
-- ข้อมูล enrichment
SELECT src_ip, src_hostname, geo_country_iso, geo_city, asn, as_org
FROM events WHERE geo_country_iso IS NOT NULL LIMIT 10;
```

```sql
-- log ที่ parser อ่านไม่ออก (ยังเก็บ raw ไว้)
SELECT ts, source_type, attrs->>'parse_error', raw FROM events WHERE NOT parse_ok LIMIT 10;
```

```sql
-- จำนวน log แยกตาม tenant และแหล่ง
SELECT t.name, e.source, count(*) FROM events e JOIN tenants t ON t.id = e.tenant_id
GROUP BY 1, 2 ORDER BY 1, 3 DESC;
```

```sql
SELECT created_at, title, status, group_value FROM alerts ORDER BY created_at DESC;
SELECT at, actor_email, action, host(src_ip), details FROM audit_log ORDER BY at DESC LIMIT 20;
SELECT at, channel, reason, host(src_ip), sample FROM ingest_drops ORDER BY at DESC LIMIT 20;
SELECT name, kind, source_type, source_cidr, enabled FROM collectors ORDER BY name;
SELECT * FROM partition_overview();
SELECT version, checksum, applied_at FROM schema_migrations;
```

ออกจาก psql: `\q`

### 2.5 ดูข้อมูลแบบที่ Viewer เห็น (ทดสอบ RLS)

```sql
SET ROLE siem_app;
SELECT count(*) FROM events;                                   -- 0 เพราะยังไม่ปักหมุด tenant
BEGIN;
SELECT set_config('app.tenant_id', (SELECT id::text FROM tenants WHERE slug = 'northwind'), true);
SELECT DISTINCT tenant_id FROM events;                          -- เห็นแค่ Northwind
COMMIT;
RESET ROLE;
```

> `SET ROLE siem_app` ต้องทำก่อน `BEGIN` เพราะในนาม `postgres` จะเห็นทุก tenant

---

## 3. API

### 3.1 ล็อกอินและเก็บ cookie

```bash
curl -c cookies.txt -X POST http://localhost:8081/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"email":"admin@siem.local","password":"admin123"}'
```

คำสั่งถัดไปใส่ `-b cookies.txt` ทุกครั้ง อ่าน JSON ให้สวยด้วย `| python -m json.tool`

```bash
curl -s -b cookies.txt "http://localhost:8081/api/events?limit=5" | python -m json.tool
```

บน VM เปลี่ยน `http://localhost:8081` เป็น `https://siem-demo.eastasia.cloudapp.azure.com`
(ต้องเป็น https เพราะ cookie ตั้งเป็น secure)

### 3.2 รายการ endpoint

| Method | Path | สิทธิ์ | ทำอะไร |
|---|---|---|---|
| GET | `/api/health` | ไม่ต้องล็อกอิน | เช็กว่า backend ทำงาน |
| POST | `/api/auth/login` | ไม่ต้องล็อกอิน | ล็อกอิน |
| POST | `/api/auth/logout` | ไม่ต้องล็อกอิน | ออกจากระบบ |
| GET | `/api/auth/me` | ล็อกอิน | ผู้ใช้ปัจจุบัน |
| GET | `/api/events` | ล็อกอิน | ค้นหา log |
| GET | `/api/events/:id/raw?ts=…` | ล็อกอิน | ข้อมูลดิบของ log หนึ่งรายการ |
| GET | `/api/stats/summary` | ล็อกอิน | ยอดรวม สำเร็จ / ล้มเหลว |
| GET | `/api/stats/timeseries?bucket=hour` | ล็อกอิน | จำนวนตามช่วงเวลา (`minute` `hour` `day`) |
| GET | `/api/stats/top?field=src_ip&top=10` | ล็อกอิน | อันดับ (`user_name` `src_ip` `event_type` `source` `geo_country_iso`) |
| GET | `/api/stats/sources` | ล็อกอิน | จำนวนแยกตามแหล่ง |
| GET | `/api/alerts` | ล็อกอิน | รายการ alert |
| POST | `/api/alerts/:id/ack` | ล็อกอิน | ปิด alert |
| GET | `/api/rules` | ล็อกอิน | กฎแจ้งเตือน |
| POST / PATCH / DELETE | `/api/rules` · `/api/rules/:id` | Admin | สร้าง / แก้ / ลบกฎ |
| GET | `/api/collectors` | ล็อกอิน | collector |
| POST / PATCH | `/api/collectors` · `/api/collectors/:id` | Admin | สร้าง / แก้ collector |
| POST | `/api/collectors/:id/rotate-token` | Admin | ออก token ใหม่ |
| GET / POST | `/api/tenants` | อ่าน: ล็อกอิน · สร้าง: Admin | tenant |
| GET / POST / PATCH | `/api/users` · `/api/users/:id` | Admin | ผู้ใช้ |
| GET | `/api/audit` | Admin | audit trail |
| GET | `/api/ingest-drops` | Admin | ข้อมูลที่ถูกปฏิเสธ |
| GET | `/api/partitions` | Admin | partition |
| POST | `/api/ingest/file` | ล็อกอิน | อัปโหลดไฟล์ log |
| POST | `/ingest` | Bearer token | ส่ง log (ไม่มี `/api` นำหน้า) |
| GET | `/api/metrics` | Admin หรือ `METRICS_TOKEN` | ค่าวัดแบบ Prometheus |

### 3.3 ตัวกรองของ `/api/events`

| พารามิเตอร์ | ตัวอย่าง |
|---|---|
| `from` `to` | `2026-09-13T00:00:00Z` |
| `outcome` | `success` `failure` `unknown` |
| `source` | `firewall` `network` `api` `crowdstrike` `aws` `m365` `ad` |
| `source_type` | `fortigate` `windows_ad` `m365` `aws_cloudtrail` `crowdstrike` `generic` |
| `event_type` · `action` · `category` | `LogonFailed` · `login` · `authentication` |
| `user` | `jsmith` |
| `ip` | `203.0.113.66` หรือ `203.0.113.0/24` |
| `country` | `TH` |
| `host` | `DC01` |
| `q` | ค้นในข้อความ |
| `tenant_id` | uuid (Admin เท่านั้น) |
| `limit` | 1–500 |
| `cursor` | ค่า `next_cursor` จากหน้าก่อน |

```bash
curl -s -b cookies.txt "http://localhost:8081/api/events?outcome=failure&ip=203.0.113.0/24&limit=20" | python -m json.tool
```

### 3.4 ส่งคำสั่งแก้ข้อมูล

POST / PATCH / DELETE ต้องมี `-H 'Content-Type: application/json'` **ทุกครั้ง แม้ไม่มี body** ไม่งั้นได้ 415

```bash
curl -b cookies.txt -X POST "http://localhost:8081/api/alerts/<alert-id>/ack" -H 'Content-Type: application/json'
```

---

## 4. ส่ง log เข้าระบบ

### 4.1 เอา token ของ collector

token แสดงครั้งเดียวตอนสร้าง ดูย้อนหลังไม่ได้ ถ้าไม่มีให้ออกใหม่:

หน้าเว็บ **Administration → Collectors → Rotate token** ที่ collector ชนิด `http`

หรือผ่าน API:

```bash
curl -s -b cookies.txt http://localhost:8081/api/collectors | python -m json.tool
curl -s -b cookies.txt -X POST "http://localhost:8081/api/collectors/<collector-id>/rotate-token" -H 'Content-Type: application/json'
```

source type ของ collector เป็นตัวกำหนดว่าใช้ parser ไหน ส่ง JSON ของ M365 ต้องใช้ token ของ collector `m365`

### 4.2 HTTP

```bash
curl -X POST http://localhost:8081/ingest \
  -H "Authorization: Bearer sk_xxxxxxxx" -H 'Content-Type: application/json' \
  -d '{"source":"api","event_type":"app_login_failed","user":"alice","ip":"203.0.113.7","@timestamp":"2026-09-13T07:20:00Z"}'
```

### 4.3 Syslog

```bash
./samples/send_syslog.sh              # ส่ง log ตัวอย่าง firewall + router
./samples/send_syslog.sh --brute      # ส่งชุดล็อกอินผิดที่ทำให้เกิด alert
./samples/send_syslog.sh --tcp        # ใช้ TCP แทน UDP
./samples/send_syslog.sh --host <IP-ของ-VM>   # ส่งไป VM
```

ต้องมี `nc` หรือ `node` ในเครื่อง

IP ผู้ส่งต้องตรงกับ CIDR ของ collector ชนิด syslog ไม่งั้นถูกทิ้ง (ดูได้ใน `ingest_drops`)
ในเครื่อง seed สร้าง `local-test-syslog` (`172.16.0.0/12`) รองรับการส่งจากเครื่องตัวเองไว้แล้ว

### 4.4 อัปโหลดไฟล์

หน้าเว็บ **Administration → Collectors → Upload a log file**

หรือ:

```bash
curl -b cookies.txt -X POST http://localhost:8081/api/ingest/file \
  -F collector_id=<collector-id> -F file=@samples/m365_audit.json
```

---

## 5. ไฟล์ JSON และ log ตัวอย่าง

| ไฟล์ | เนื้อหา | collector ที่ต้องใช้ |
|---|---|---|
| `samples/firewall_syslog.log` | โจทย์ข้อ 4.1 + FortiGate | syslog `fortigate` |
| `samples/network_syslog.log` | โจทย์ข้อ 4.2 + sshd | syslog `generic` |
| `samples/api_login.json` | โจทย์ข้อ 4.3 | http `generic` |
| `samples/crowdstrike.json` | โจทย์ข้อ 4.4 | `crowdstrike` |
| `samples/aws_cloudtrail.json` | โจทย์ข้อ 4.5 + CloudTrail จริง | `aws_cloudtrail` |
| `samples/m365_audit.json` | โจทย์ข้อ 4.6 + M365 จริง | `m365` |
| `samples/windows_ad.json` | โจทย์ข้อ 4.7 + Event 4624 | `windows_ad` |

อ่านไฟล์ JSON ให้สวย:

```bash
python -m json.tool samples/m365_audit.json
```

ส่งไฟล์ JSON ทั้งหมดเข้าระบบ:

```bash
python samples/post_logs.py --token sk_xxxxxxxx
python samples/post_logs.py --token sk_xxxxxxxx --file m365_audit.json
python samples/post_logs.py --token sk_xxxxxxxx --simulate 200
python samples/post_logs.py --token sk_xxxxxxxx --url https://siem-demo.eastasia.cloudapp.azure.com/ingest
```

ไฟล์ตัวอย่างลงวันที่ปี 2025 สคริปต์จะเปลี่ยนเวลาเป็นปัจจุบันให้เอง
ถ้าใส่ `--keep-timestamps` จะส่งวันที่เดิม ซึ่ง **ไม่ขึ้นบน dashboard** เพราะเก่ากว่าช่วงที่แสดงและเกิน retention 7 วัน

อัปโหลดผ่านหน้าเว็บและ `POST /ingest` ก็ขยับเวลาให้เหมือนกันเมื่อทั้งชุดเก่ากว่า retention (เวลาเดิมอยู่ที่ `attrs.original_ts`) ถ้าต้องการวันที่เดิมให้ติ๊ก "Keep original" หรือส่ง `-F keep_timestamps=true` / `-H "X-Keep-Timestamps: true"`

ดูข้อมูลดิบของ log หนึ่งรายการผ่านหน้าเว็บ: **Search → คลิก "raw payload"** ใต้ข้อความ

---

## 6. log ของ container

| คำสั่ง | ดูอะไร |
|---|---|
| `docker compose logs -f backend` | log ของ backend: migration, syslog, alert, error |
| `docker compose logs web` | log ของ nginx |
| `docker compose logs db` | log ของ PostgreSQL |
| `docker compose logs --since 10m backend` | เฉพาะ 10 นาทีล่าสุด |

บน VM ใส่ `sudo` และ `-f docker-compose.yml -f docker-compose.caddy.yml` แล้วดู Caddy เพิ่ม:

```bash
sudo docker compose -f docker-compose.yml -f docker-compose.caddy.yml logs caddy
```

ข้อความใน backend ที่ควรรู้

| ข้อความ | ความหมาย |
|---|---|
| `[boot] API listening on :8080` | บูตสำเร็จ |
| `[migrate] applied …` | รัน migration ใหม่ |
| `[migrate] updated recorded checksum after comment-only change` | ปรับ checksum ของ migration เดิมให้อัตโนมัติ ปกติ |
| `was modified after it was applied` | **บูตไม่ได้** มีคนแก้ migration ที่รันไปแล้ว |
| `[ingest] dropped syslog/… no collector matches sender` | IP ผู้ส่งไม่ตรง collector |
| `[enrich] no GeoIP database` | ยังไม่ได้โหลดไฟล์ GeoIP ระบบยังทำงานปกติ |
| `[alerting] raised N new alert(s)` | เกิด alert |

---

## 7. ไฟล์ตั้งค่า, ใบรับรอง, GeoIP, volume

| สิ่งที่ดู | ที่อยู่ / คำสั่ง |
|---|---|
| ค่าตั้งทั้งหมด | ไฟล์ `.env` (ในเครื่อง `E:\Demo_test\.env` · บน VM `~/mini-siem/.env`) มีรหัสผ่าน **อย่า commit และอย่าส่งให้ใคร** |
| ค่าที่ compose ใช้จริง | `docker compose config` |
| ใบรับรองในเครื่อง (self-signed) | `docker compose exec web cat /etc/nginx/certs/server.crt \| openssl x509 -noout -subject -dates` |
| ใบรับรองบน VM (Let's Encrypt) | `echo \| openssl s_client -connect siem-demo.eastasia.cloudapp.azure.com:443 2>/dev/null \| openssl x509 -noout -issuer -dates` |
| ไฟล์ GeoIP | `geoip/dbip-city-lite.mmdb` — โหลดด้วย `bash scripts/fetch-geoip.sh` แล้ว `docker compose restart backend` |
| volume ในเครื่อง | `docker volume ls` → `demo_test_pgdata` (ฐานข้อมูล) · `demo_test_tlscerts` (ใบรับรอง) |
| volume บน VM | `mini-siem_pgdata` · `mini-siem_caddydata` (ใบรับรอง Let's Encrypt — **อย่าลบ** ขอใหม่บ่อยจะติด rate limit) |
| เข้าไปดูใน container | `docker compose exec backend sh` · `docker compose exec web sh` |

---

## 8. สำรองและส่งออกข้อมูล

สำรองทั้งฐานข้อมูล:

```bash
docker compose exec -T db pg_dump -U postgres siem > backup.sql
```

ส่งออก log เป็น CSV:

```bash
docker compose exec -T db psql -U postgres -d siem \
  -c "\copy (SELECT ts, source, event_type, event_outcome, user_name, host(src_ip) FROM events ORDER BY ts DESC LIMIT 1000) TO STDOUT CSV HEADER" > events.csv
```

---

## 9. เทสต์

```bash
cd backend && npm test
```

เทสต์ความปลอดภัย (RLS และลบ log ไม่ได้) ต้องมีฐานข้อมูลรันอยู่ ไม่งั้นจะข้ามไปเอง:

```bash
docker compose up -d db
cd backend && POSTGRES_HOST=localhost POSTGRES_PORT=5433 npx vitest run test/isolation.test.ts
```

---

## 10. สิ่งที่ห้ามทำ

| ห้าม | เพราะ |
|---|---|
| `UPDATE` / `DELETE` ตาราง `events` หรือ `audit_log` ในนาม `postgres` | superuser ทำได้จริง แต่ทำลายหลักฐาน กฎ "ลบไม่ได้" ใช้กับ role ของแอปเท่านั้น |
| แก้ไฟล์ใน `db/migrations/` ที่รันไปแล้ว | backend บูตไม่ได้ ให้สร้างไฟล์ migration ใหม่แทน |
| `./run.sh clean` หรือ `docker compose down -v` | ลบข้อมูลทั้งหมด |
| ลบ volume `caddydata` บน VM | ใบรับรองหาย ขอใหม่บ่อยติด rate limit ของ Let's Encrypt |
| เปิด collector `public-syslog-any-*` ทิ้งไว้ | ใครก็ส่ง syslog เข้า tenant Northwind ได้ |
| เปิด VM ทิ้งไว้ | เสียเงิน $0.105 ต่อชั่วโมง |
